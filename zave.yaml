apiVersion: zave.io/v1
kind: Workload
metadata:
  name: rigoberta
spec:
  runtime: container
  exposure: public-http
  delivery: rolling

  build:
    mode: dockerfile

  persistence:
    engine: postgres

  capabilities:
    - name: metrics

  resources:
    tier: standard
